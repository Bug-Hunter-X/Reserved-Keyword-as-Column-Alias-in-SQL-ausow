To avoid the syntax error, choose a non-reserved keyword for your column alias.

```sql
SELECT column1 AS column1_alias FROM my_table;
```

Here, `column1_alias` is a more appropriate alias that won't clash with SQL's reserved keywords.